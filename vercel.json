{
  "installCommand": "npm install",
  "buildCommand": "prisma generate && prisma migrate deploy && next build",
  "crons": [
    {
      "path": "/api/cron/dialog-reminders",
      "schedule": "0 8,20 * * *"
    }
  ]
}